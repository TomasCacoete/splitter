#ifndef SPLITTER_H
#define SPLITTER_H

#include <SDL2/SDL.h>

struct SplitterData {
    //homePage
    SDL_Surface* logo;
    SDL_Surface* pdfImage;
    SDL_Surface* videoImage;
};

#endif